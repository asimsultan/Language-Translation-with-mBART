# mBART Language Translation

This project fine-tunes mBART for language translation using the WMT dataset.

## Requirements

- transformers
- torch
- datasets

Install the dependencies:

```bash
pip install -r requirements.txt